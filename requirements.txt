# ---------------------------------------------------------------------------
# Package requirements for the Abhishekth Reddy Thanam portfolio website.
#
# NOTE: This is a Node.js / Next.js project, NOT a Python project.
# These packages are managed by npm via package.json + package-lock.json.
#
# To install everything, you do NOT use pip. Run:
#
#     npm install
#
# Then start it with:
#
#     npm run dev      # development server at http://localhost:3000
#     npm run build    # production build
#     npm run start    # serve the production build
#
# Prerequisite: Node.js 18.18+ (Node 20 or 22 LTS recommended) and npm.
#   Download Node (which includes npm): https://nodejs.org
#
# The exact versions below mirror package.json for quick reference.
# ---------------------------------------------------------------------------

# Runtime dependencies
next==16.2.9
react==19.2.0
react-dom==19.2.0

# Development dependencies (build, types, linting, styling)
typescript>=5
@types/node>=20
@types/react>=19
@types/react-dom>=19
tailwindcss>=4
@tailwindcss/postcss>=4
eslint>=9
eslint-config-next==16.2.9
